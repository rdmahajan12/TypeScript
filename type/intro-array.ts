// Array - In typescript, an array is a data type that can store multiple values of different data types sequentially.
// a. using square brackets -
const number: number[] = [1, 2, 3, 4, 5];

// b. Using the array constructor -
const number2: number[] = new Array(1, 2, 3);

// c. Using the Array.of method -
const names: string[] = Array.of("ritesh", "js", "react");

console.log(names.length);
